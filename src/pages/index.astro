---
/**
 * Root redirect to default locale
 * This ensures proper SEO by redirecting to the canonical localized URL
 */
import { defaultLang } from "@/i18n/ui";

return Astro.redirect(`/${defaultLang}/`, 301);
---
