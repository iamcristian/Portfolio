---
import { getLangFromUrl, useTranslations } from "@/i18n/utils";
import { Image } from "astro:assets";
import meImage from "@/assets/me.webp";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const about = t("about");
---

<section id="about" class="py-18 lg:py-32 px-6 lg:px-8">
  <div class="max-w-6xl mx-auto">
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-16 items-center">
      <div class="space-y-6 col-span-2">
        <h2 class="text-2xl md:text-3xl font-bold tracking-tight">
          {about.title}
        </h2>
        <p>{about.paragraph1}</p>
        <p>{about.paragraph2}</p>
        <p>{about.paragraph3}</p>
        <div class="stats shadow flex justify-center text-center">
          {
            about.stats.map((stat) => (
              <div class="stat">
                <div class="stat-value">{stat.number}</div>
                <div class="stat-title">{stat.title}</div>
              </div>
            ))
          }
        </div>
      </div>
      <div class="px-40 lg:px-0">
        <Image
          src={meImage}
          alt={about.title}
          class="w-full h-auto rounded-2xl shadow-lg"
          loading="lazy"
          width={600}
          height={600}
        />
      </div>
    </div>
  </div>
</section>
